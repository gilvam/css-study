import { ActualWidthDirective } from './actual-width.directive';

describe('ActualWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new ActualWidthDirective();
    expect(directive).toBeTruthy();
  });
});
